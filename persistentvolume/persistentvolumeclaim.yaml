apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: example-claim
spec:
  storageClassName: default-storageclass
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Mi